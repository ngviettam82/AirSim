# This repository intentionally contains mixed LF/CRLF files.
# Preserve the exact line endings committed in Git on every platform.
* -text

*.mp4 filter=lfs diff=lfs merge=lfs -text
*.pdf filter=lfs diff=lfs merge=lfs -text
